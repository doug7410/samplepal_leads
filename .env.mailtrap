# Mailtrap Configuration
MAIL_MAILER=smtp
MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=your_mailtrap_username
MAIL_PASSWORD=your_mailtrap_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=angela@samplepall.net
MAIL_FROM_NAME="Sample Pal"

# Make sure failover uses mailtrap instead of resend
# Add these lines to your .env file to override the current configuration